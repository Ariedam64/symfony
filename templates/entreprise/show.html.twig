{% extends 'base.html.twig' %}

{% block titreStage %}Consulter une entreprise{% endblock %}

{% block contenuSupp %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entreprise.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entreprise.nom }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ entreprise.activite }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ entreprise.adresse }}</td>
            </tr>
            <tr>
                <th>SiteWeb</th>
                <td>{{ entreprise.siteWeb }}</td>
            </tr>
        </tbody>
    </table>

    <a class="nav-link" href="{{path('entreprise_edit', {'id': entreprise.id}) }}"><button type="submit" class="btn btn-dark">Modifier</button></a>

    <a class="nav-link" href="{{path('entreprise_index') }}"><button type="submit" class="btn btn-primary">Revenir à la liste</button></a>

    {{ include('entreprise/_delete_form.html.twig') }}

{% endblock %}
