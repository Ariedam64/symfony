{% extends 'base.html.twig' %}

{% block titreStage %}Stage{% endblock %}

{% block contenuSupp %}

    <table class="table">
        <tbody>
            <tr>
                <th>Intitule</th>
                <td>{{ stage.intitule }}</td>
            </tr>
            <tr>
                <th>Mission</th>
                <td>{{ stage.mission }}</td>
            </tr>
            <tr>
                <th>Adresse mail</th>
                <td>{{ stage.adresseMail }}</td>
            </tr>
        </tbody>
    </table>

      <a class="nav-link" href="{{path('stage_edit', {'id': stage.id}) }}"><button type="submit" class="btn btn-dark">Modifier</button></a>

    <a class="nav-link" href="{{path('stage_index') }}"><button type="submit" class="btn btn-primary">Retour à la liste</button></a>



    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
